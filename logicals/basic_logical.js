// WAP as check min and max number?

// Approach one
const numbers = [122, 34, 400, 778, 10, 876];
// const max = Math.max(...numbers);
// const min = Math.min(...numbers);

// console.log("max : ", max);
// console.log("min : ", min);

// Approach two
let max = number[0];
let min = number[0];
for (let i = 1; i < numbers.length; i++) {
  if (number[i] > max) {
    max = num[i];
  }
  if (numbers[i] < min) {
    min = numbers[i];
  }
}
console.log("max : ", max);
console.log("min : ", min);

//Reverse String

const strValue = "Ravi Ranjan";
let revStr = "";
